import fastapi, psycopg2, redis, requests
print("✅ All main Python packages are successfully installed!")

import os
os.system("docker --version")
os.system("nginx -v")
os.system("psql --version")

